# Fail2Ban filter for login brute force attempts
# Matches POST requests to /login that return 422 (validation) or 302 (failed redirect)

[Definition]
failregex = ^<HOST> -.*"POST /login\b.*" (401|403|422)
            ^<HOST> -.*"POST /register\b.*" (401|403|422|429)
ignoreregex =
