<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default Institution ID
    |--------------------------------------------------------------------------
    |
    | Used when automatically assigning institutions (e.g., during social
    | sign-up). Set DEFAULT_INSTITUTION_ID in your environment if you want to
    | control which institution new users belong to; otherwise, the first
    | active institution will be used.
    |
    */
    'default_id' => env('DEFAULT_INSTITUTION_ID'),
];
